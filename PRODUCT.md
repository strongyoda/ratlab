# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 주 사용자: 뇌동맥류(SAH) 동물모델 연구를 수행하는 연구자 본인과 랩 동료들. 여러 명이 같은 계정 체계로 로그인해 기록하고 열람한다.
- 두 가지 사용 장면이 모두 핵심이다:
  1. **현장 기록 (모바일)** — 동물실에서 스마트폰으로 체중·상태·투여·물통 무게 등을 입력. 빠르고 실수 없는 입력이 최우선.
  2. **데스크탑 분석** — 책상에서 코호트 그래프(체중·SBP·생존 등)를 확인하고 논문용 데이터를 정리.

## Product Purpose

랫드(rat) 실험동물의 사육·수술·투여·측정 기록을 한 곳에서 관리하고, 코호트 단위 분석까지 이어주는 랩 전용 웹앱 (Rat Lab Manager Pro / RLM Pro).

성공 기준 (사용자 확인, 우선순위 순):

1. **기록 무결성** — 현장에서 빠르고 실수 없이 입력되어, 논문에 쓸 수 있는 신뢰도 높은 원자료가 쌓이는 것.
2. **논문용 분석·그래프** — 체중·SBP·생존 곡선 등을 논문 그림 수준으로 뽑는 것.
3. **동물 이상 조기 발견** — 체중 급감·섭취 저하 등 위험 개체를 빨리 알아채는 것.

## Operating Context

- 연구 도메인: BAPN 유도 뇌동맥류/SAH 모델. 코호트(C13, C14 등) 단위로 반입 → 수술(결찰, OVX) → 투여(메트포민 등) → BP/체중/섭취량 측정 → 희생·부검(COD 분류: Neurological/Non-Neurological, Aneurysm O/X, SAH·Infarction·Vasospasm 등)의 흐름.
- 주요 워크플로: 케이지별 입력, 일괄 입력(수술·OVX·MR·희생), 상태&체중 기록, BP 계산&입력, 섭취량·투여량 분석, 코호트 분석/비교, 조건분석, 데이터 관리.
- 배포·편집 방식: 사용자는 GitHub 웹에서 파일을 직접 수정·커밋하는 방법만 사용한다. 로컬 빌드 과정을 돌릴 수 없다. **GitHub의 저장소가 실서비스이고, 이 로컬 폴더는 백업 사본이다** — 로컬 수정이 실서비스에 자동 반영되지 않는다.
- 백엔드: Firebase (Firestore + Auth + Storage + Functions, 프로젝트 `nidd-lab`).

## Capabilities and Constraints

- 스택: 순수 HTML/CSS/JS 단일 페이지 앱(탭 기반 자체 라우터), Firebase v9 compat SDK, Chart.js(+sankey, zoom, hammer). 빌드 단계 없음.
- **빌드 필요 없는 구조 유지** — 사용자는 GitHub 웹 커밋만 가능하므로, 번들러·트랜스파일 등 로컬 빌드가 필요한 도구는 도입 불가. (빌드 없이 동작하는 방식이면 허용.)
- **기존 Firestore 데이터 절대 불변** — 지금까지 쌓인 데이터는 절대 건드리면 안 된다. 스키마(문서 구조)를 바꿔야 하는 작업은 반드시 사용자에게 먼저 물어보고 승인받아야 한다.
- **Firestore 읽기 절약 필수** — 무료 할당량/비용 때문에 캐시(5분 캐시 등 기존 패턴)를 유지하고 불필요한 쿼리를 만들지 않는다.
- 언어: UI 전체가 한국어(도메인 용어는 영문 혼용: SAH, COD, ARE, OVX, MR, SBP 등).
- 모바일 웹이 1급 시민: viewport 고정, 터치 인터랙션(hammer.js), 사이드 드로어 메뉴.

## Brand Commitments

- 제품명: **Rat Lab Manager Pro**, 화면 표기 약칭 **RLM Pro**.
- 그 외 확정된 시각 아이덴티티 약속 없음(현행 스타일은 코드가 기록하는 incumbent 상태일 뿐, 사용자가 binding으로 못박은 것은 아님).

## Evidence on Hand

- 실데이터: Firestore `nidd-lab` 프로젝트에 코호트 다수(C13, C14, 파일럿 등)의 체중·BP·투여·COD 기록이 축적되어 있음. 이것이 논문의 원자료다.
- 로컬 백업: `backup_c13_cod_2026-08-30.json`, `4월 22일 백업/`, `8월 10일 백업/` 디렉터리.
- 가짜 데이터·가짜 실적을 만들어내면 안 됨: 화면에 보이는 수치는 전부 실제 실험 기록이다.

## Product Principles

1. **현장 입력은 장갑 낀 손으로도** — 모바일 입력 흐름은 탭 수·오입력 가능성을 늘리는 어떤 변경도 하지 않는다.
2. **데이터가 곧 논문이다** — 저장 로직·스키마에 손대는 변경은 보수적으로, 반드시 승인 후에.
3. **읽기 한 번도 아껴 쓴다** — 기능 추가가 Firestore 쿼리 증가를 수반하면 캐시 전략부터 설계한다.
4. **GitHub 웹 커밋으로 굴러가는 앱** — 도구·구조 선택은 "사용자가 브라우저에서 파일 하나 고쳐 커밋할 수 있는가"를 통과해야 한다.
5. **분석 화면은 논문 그림의 초안** — 그래프는 예쁜 대시보드가 아니라 학술 그림 기준(축·단위·군 구분 명확)으로 판단한다.
